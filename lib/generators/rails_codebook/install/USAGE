Description:
    Generates few of basic codebooks country/currency/importance/boolean

Example:
    rails generate rails_codebook:install

    This will create:
        create  db/seeds/default_codebooks.rb
        rake    db:seed